# map-it can be convert or execute variable by one by one
# 1)
# lst=['1','3','4','6']        #all elements can change its functions
# res=map(int,lst)             #synatx res=map(fun,sequence)
# print(list(res))
